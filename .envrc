# Auto-prepend project venv to PATH when entering this directory (requires direnv).
# Without direnv: source .venv/bin/activate  OR  bin/pytest  OR  make test-glossary
export VIRTUAL_ENV="$PWD/.venv"
PATH_add .venv/bin
